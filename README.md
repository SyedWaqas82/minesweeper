# Minesweeper

## Setup

Install the app dependencies:

```
bundle install
rails db:setup
rails webpacker:install
rails webpacker:install:react
```

## Tests

To run all tests:

```
bundle exec rspec
```
